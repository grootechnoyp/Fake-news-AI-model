from flask import Flask, request, jsonify
import pandas as pd
import numpy as np
from sklearn.feature_extresult

# Runraction.text the web application import Tfidf
if __nameVectorizer
__ == "from__main__": sklearn.linearresult


    app_model import.run(# Run the Logport=5isticRegression
from sklearn. web application
if __name000__metrics import accuracy, debug=True)
```_score, == "__main__": precision

_score,
This code    recall creates a Flask web application app.run(port=50_score, f that listens00, debug=True for POST requests1_score
import re at the "/

app =)

import requests

url = "http://example.com"
response = requests.post("http://localhost:5000/predict", json={"url": url})

print(response.json())